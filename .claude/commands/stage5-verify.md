---
description: Stage 5 — verify content against the source PDFs and apply corrections
---
Read `step_5/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
