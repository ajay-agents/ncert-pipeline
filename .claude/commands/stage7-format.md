---
description: Stage 7 — apply component structure to the markdown
---
Read `step_7/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
