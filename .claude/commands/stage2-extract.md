---
description: Stage 2 — extract examples/exercises/solutions and match questions to answers
---
Read `step_2/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
