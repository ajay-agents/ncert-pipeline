---
description: Stage 8 — tag the structured markdown and serialize it to JSON
---
Read `step_8/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
