---
description: Stage 1 — convert chapter and solutions PDFs to markdown via Mathpix
---
Read `step_1/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
