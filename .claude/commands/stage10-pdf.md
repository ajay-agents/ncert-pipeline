---
description: Stage 10 — export the finished design page to a fixed PDF
---
Read `step_10/PROMPT.md` and follow it exactly, substituting `$ARGUMENTS` for
every `$ARGUMENTS` placeholder in that file (it is the chapter directory name,
e.g. `physics-11-10`).
